#include "uploader.h"

bool Uploader::upload_buffer(const Buffer& buffer, void* memory, size_t size, vk::DeviceSize offset) {

}

bool upload_image(
    const Texture& texture, 
    const Image& image, 
    vk::Offset2D offset = vk::Offset2D(0, 0)
) {

}